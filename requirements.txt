requests>=2.31.0
python-dateutil>=2.8.0
python-dotenv>=1.0.0
flask>=3.0.0
flask-cors>=4.0.0

# Telegram bot
python-telegram-bot>=22.0

# Web search (DuckDuckGo — no key needed)
duckduckgo-search>=8.0

# TTS — layered, use what you have
edge-tts>=7.0           # free, Microsoft servers
kokoro-onnx>=0.4.0      # local neural TTS, ~80MB model auto-downloads on first use
soundfile>=0.12.0       # required by kokoro-onnx
# pyttsx3               # optional offline fallback (install manually if needed)
