requests>=2.31.0,<3.0
psutil>=5.9.0,<7.0          # CPU/memory monitoring for adaptive throttling
python-dateutil>=2.8.0,<3.0
python-dotenv>=1.0.0,<2.0
flask>=3.0.0,<4.0
flask-cors>=4.0.0,<7.0

# Telegram bot
python-telegram-bot>=21.0,<23.0

# Web search (DuckDuckGo — no key needed)
duckduckgo-search>=6.0,<9.0

# TTS — layered, use what you have
edge-tts>=6.0,<8.0          # free, Microsoft servers
kokoro-onnx>=0.4.0,<1.0     # local neural TTS, ~80MB model auto-downloads on first use
soundfile>=0.12.0,<1.0      # required by kokoro-onnx
# pyttsx3                   # optional offline fallback (install manually if needed)
